# Follow-ups — by-stage punch list

A running log of known gaps that are intentionally deferred to a later stage. Each entry names the stage it belongs in, the decision that deferred it, and the code/schema location that should be revisited. This file is how we keep tech debt visible between stages without cluttering the per-stage spec.

Convention: when you land the fix, delete the bullet here AND the matching `TODO(...)` comment in code.

---

## S1 → S2

- ~~**Implement `pnpm trace` REQ-ID coverage check.**~~ Shipped 2026-04-18 in [scripts/trace-req-ids.mjs](../scripts/trace-req-ids.mjs). Strict by default (exits 1 on missing); set `TRACE_VERBOSE=1` for per-REQ coverage, `TRACE_STRICT=1` to also fail on zombie REQ-IDs in tests that no spec §4 claims.

## Batched verification queue (run before next submission-gate checkpoint)

Rationale: heavy smoke checks (docker compose --build, full browser flow) stall feature development when run per-feature. Queue them and run in a single pass after every 2–3 feature merges, or immediately before a submission-gate checkpoint.

- **Docker-compose submission-gate smoke.** `docker compose down -v && docker compose up --build --abort-on-container-exit` from repo root — confirm migrate + seed exit 0, backend logs `Listening on 0.0.0.0:4000`, web serves `/login` with HTTP 200. Last confirmed: before `feat/s1-rooms`. Re-run after: s1-rooms merge + next 1–2 features. Steps codified in [SMOKE.md §1–§2](./SMOKE.md).
- **Manual curl smoke for new write endpoints** (create room + leave room happy/403 per [plans/2026-04-19-s1-rooms.md](./plans/2026-04-19-s1-rooms.md#task-10) Step 5). Integration tests cover the behavior; the curl pass just re-confirms the containerized stack wires auth cookies + route prefix `/api/v1` correctly end-to-end. Low incremental value when tests are green; bundle with the docker smoke above.
- **Playwright multi-browser multi-user** (per `feedback-playwright-multi-user` memory — Chrome + Firefox to exercise concurrent sessions on the same feature). Run at the same checkpoint as the docker smoke so the same running stack serves both.
- ~~**Backend sign-up rate-limit bleed across test files.**~~ Shipped 2026-04-20 on `chore/test-infra`: `auth.ts`'s `customRules["/sign-up/email"]` is now a function that returns `max = 10000` when `NODE_ENV === "test"` (falls through to prod `5` otherwise), and `registerRateLimitGuard` in `app.ts` short-circuits under the same flag so supertest's always-127.0.0.1 source can't poison the cross-file `/24` subnet bucket. An explicit test-only escape hatch (`__setTestSignUpMaxOverride`, mirroring the `__setTestRateLimitGlobalMax` pattern) restores the production 5-per-hour ceiling inside `tests/register-rate-limit.test.ts` so REQ-009's 429 path is still fenced by a real-code assertion. `LOG_LEVEL` default in `tests/setup.ts` bumped from `warn` → `error` to mute the 600+ `auto-enroll skipped: 'general' room not seeded` lines that made the auth.ts warn dominate CI output; the warn path is still covered explicitly by `register-auto-enroll.test.ts`. Originally observed merging `feat/s2-room-mgmt-ui` where `room-patch.test.ts` + `room-delete.test.ts` both green solo chained into 404s once the 5-per-3600s bucket exhausted.
- **In-browser Alice/Bob smoke for S2 room-mgmt** (create room, rename as owner, delete as owner, leave as member, foreign tab receives `room.deleted` and navigates out). Deferred per batched-smoke SOP; fold into the next docker-compose checkpoint.
- **In-browser Alice/Bob smoke for S2 presence** (three-state green/yellow/grey flip within 2s, MemberList pill + Header self-pill). Deferred per batched-smoke SOP; fold into the next docker-compose checkpoint alongside room-mgmt.
- **Playwright e2e for S2 invitations** ([tests/e2e/s2-invitations.spec.ts](../tests/e2e/s2-invitations.spec.ts)) — run at the next docker-compose checkpoint. Two scenarios across two BrowserContexts: (a) happy path Alice-invites-Bob-accepts lands Bob inside the private room; (b) cancel asymmetry — Alice cancels and Bob's `[data-testid=inbox-list]` drops the row live (proves R7 fanout routes to `user:{inviteeId}`). Spec self-guards with a `beforeAll` health probe at `:4000/health`. Dual-browser (Chrome+Firefox per `feedback-playwright-multi-user`) not wired yet — today both contexts come from the same browser; swap the second `browser.newContext()` for `firefox.launch()` or add a `projects` entry in `playwright.config.ts` at the gate checkpoint if true cross-browser coverage is required.
- ~~**Backend parallel-DB FK race across test files — primary symptom.**~~ Shipped 2026-04-20 on `chore/test-infra`: root-caused NOT as a truncation-order race but as the same better-auth `/sign-up/email` bucket bleed above. When that bucket exhausts mid-test, better-auth's sign-up handler silently skips the Set-Cookie header; the test's supertest agent then has no session, later follow-up INSERTs reference a user-id the test *thinks* it created but whose session never attached — surfacing as FK-looking 401/404 cascades. With the bucket bump in place, solo `tests/users-unban.test.ts` goes from 4 fails / 47s (tests 3-6 timeout at 15s hookTimeout) to 6-pass / ~1s, and the representative full-run tallies pre- vs post-fix shift from 44-65 failing files to 0-2 flaky files per run. **Residual, not a regression**: occasional 1-11 tests across 1-2 files still fail on rerun with "user not found" / 404 / 15s timeouts (observed 659/670 on one of four post-fix runs, vs 670/670 on two others). This residual correlates with wall-clock duration doubling (89s → 195s on the bad run), suggesting host-level container thrash rather than a deterministic truncate-pool race. Parked as batched-smoke follow-up, no longer a single-file-solo repro.
- **Dual-browser smoke for S2 active-sessions UI** ([apps/web/src/app/settings/sessions/page.tsx](../apps/web/src/app/settings/sessions/page.tsx)) — run at the next docker-compose checkpoint once the wave-1 smoke image is rebuilt. Steps per [docs/briefs/wave1-sessions.md](./briefs/wave1-sessions.md#gate-criteria): Alice signs in on Chrome + Firefox, Chrome `/settings/sessions` shows both rows with Chrome tagged "This browser", revoke the Firefox row → Firefox hits 401 on next `/rooms` fetch and RequireSession redirects to `/login`, revoke the Chrome row via "Sign out this browser" → Chrome redirects to `/login`, and `DELETE /api/v1/sessions/{bogus-id}` in devtools returns 403. Deferred per batched-smoke SOP; merge (`f1dfc43`) happened against the orphaned Docker e2e stack so the running image doesn't yet include the new route. Dual-browser (Chrome+Firefox per `feedback-playwright-multi-user`) — one `browser.newContext()` + one `firefox.launch()`, or two real browsers manually, per the invitations entry above.
- **REQ-208 dual-browser moderation smoke for `feat/room-roles`.** Per `docs/specs/s2-moderation.md` §6 Task 8: Alice (Chrome) + Bob (Firefox) + Carol (Edge) — Carol kicks Bob → Bob's socket drops and rejoin returns 403; Alice unbans Bob → Bob rejoins and sees history; Alice demotes Carol → Carol's action buttons disappear live; Alice tries to demote herself → 403. Deferred per batched-smoke SOP.
  - **Coverage already in place**: REQ-208 server-side dual-socket force-leave is locked in by `apps/backend/tests/room-moderation-kick.test.ts` (all 11 tests green, including "REQ-208 dual-socket kick: both target sockets force-leave room within 500 ms"). The deferred work is the hands-on cross-browser UX sweep, not the protocol guarantee.
  - **When to run**: next submission-gate checkpoint, alongside the room-mgmt + presence browser smokes queued above. Dual-browser (not just multi-context) is required per memory `feedback-playwright-multi-user` since Playwright MCP's single BrowserContext collapses to one cookie jar.
  - **Raised**: 2026-04-19 at end of `feat/room-roles` Wave 1 sprint (Task 8).
- ~~**Italic + `title` tooltip polish on attachment comment render (REQ-E-UI-LIST-COMMENT).**~~ Shipped 2026-04-20 on `chore/post-event-cleanup`: `AttachmentChip.tsx` and `AttachmentImage.tsx` now render the comment with `italic`, `title={attachment.comment}`, and `truncate max-w-[18rem]` per spec [s2-attachments-enhance.md §5.2](./specs/s2-attachments-enhance.md). Dual-browser smoke for the caption-display flow still folds into the next batched docker-compose checkpoint.
- ~~**Attachments download 404-vs-403 probe-oracle tightening (§2.6.4 defense-in-depth).**~~ Shipped 2026-04-20 on `chore/cleanup-error-codes`: [routes/attachments.ts](../apps/backend/src/routes/attachments.ts) GET handler now collapses the missing-row branch into the same 403 forbidden as the gate-fail branch. Test renamed from `REQ-083 unknown id → 404` to `§2.6.4 unknown id → 403 (probe-oracle suppression)` in [attachments-download.test.ts](../apps/backend/tests/attachments-download.test.ts). Spec [s2-attachments-enhance.md §2 non-goals](./specs/s2-attachments-enhance.md) updated in lockstep — the 404-on-unknown-id non-goal is now retracted.
- ~~**Pre-existing `packages/shared/src/dto-room.test.ts` vitest-types error.**~~ Resolved 2026-04-20 — workspace-wide `pnpm typecheck` is now fully green (3/3 projects). The test file imports `describe/expect/test` from `vitest` explicitly, which sidesteps the missing-globals path.
- **S2 e2e scaffolding runtime verification.** Branch `feat/e2e-scaffolding` (worktree `../hackaton-e2e-scaffolding`) ships [docs/specs/s2-e2e-coverage.md](./specs/s2-e2e-coverage.md) plus eight new Playwright spec files under [tests/e2e/](../tests/e2e/). Static counts (from `grep '^  test(' tests/e2e/`): **11 live tests** (REQ-017/018 sessions ×3, REQ-201..211 moderation ×4, REQ-089 invitation-decline ×1, REQ-110/133 replies ×3) and **5 skipped** (agent E `test.skip` ×2 for §2.6.3/§2.6.4, agent F `test.skip` ×3 for §2.4.3/§2.5.2/§2.7). `pnpm test:e2e --list` + `pnpm trace` not run in the scaffolding worktree because `node_modules` is not installed there (deliberate — the worktree is docs + test sources only, no deps). Fold into the next docker-compose checkpoint: `pnpm install` on the branch after ff-merge to main, then `pnpm test:e2e --list` (expect ≥11 runnable + 5 skipped) and `pnpm trace` (expect REQ-017/018/089/110/133/201..211 all green). E/F skeleton names carry `REQ-TBD` until E and F merge and replace the placeholder with their canonical REQ-IDs. Raised 2026-04-19 at end of agent-G scaffolding sprint. ff-merged to main 2026-04-19 (commit `9c595bf`).
- **Invitations R11 — pill vs. row-removal is a product choice, not a regression.** [docs/specs/s2-e2e-coverage.md](./specs/s2-e2e-coverage.md) §4 R11 and [tests/e2e/s2-invitations-decline.spec.ts](../tests/e2e/s2-invitations-decline.spec.ts) were written against the shipped behavior in [apps/web/src/components/rooms/InvitationsTab.tsx:62-79](../apps/web/src/components/rooms/InvitationsTab.tsx#L62-L79): on `room.invitation.declined` the inviter's pending row is removed client-side (no "declined" status badge). If product wants the pill back in place of the silent removal, it's a post-hackathon UX item — not an S2 regression and not blocking any REQ in the v3 brief. No fix scheduled; log-and-park. Raised 2026-04-19 alongside the e2e scaffolding merge.

## S2 → S3

- ~~**Room moderation per-action rate limit.**~~ Shipped 2026-04-20 on `feat/s3-gc-and-moderation-rl`: [apps/backend/src/lib/room-moderation-rate-limit.ts](../apps/backend/src/lib/room-moderation-rate-limit.ts) exports `checkModerationRateLimit(userId, roomId)` with dual-tier Redis counters (burst 10/60s + sustained 60/3600s). INCR-order discipline (burst first; GET sustained on burst-block; DECR burst on sustained-block) prevents a 1-minute block from cascading into a 1-hour jail. Wired into all five moderation handlers in [apps/backend/src/routes/rooms.ts](../apps/backend/src/routes/rooms.ts) (promote/demote/kick/ban/unban) AFTER `requireFriendshipAuth` and BEFORE any DB read, returning `{error:"rate_limited", retryAfterSec}` + WARN log. Redis-outage policy: fail-open. Covered by [apps/backend/src/lib/room-moderation-rate-limit.test.ts](../apps/backend/src/lib/room-moderation-rate-limit.test.ts) (5 unit tests) + [apps/backend/tests/rooms-moderation-rate-limit.test.ts](../apps/backend/tests/rooms-moderation-rate-limit.test.ts) (5 integration tests, one per endpoint). Spec: [docs/specs/s3-gc-and-moderation-rl.md](./specs/s3-gc-and-moderation-rl.md).

- ~~**Attachment orphan GC.**~~ Shipped 2026-04-20 on `feat/s3-gc-and-moderation-rl`: [apps/backend/src/lib/attachment-gc.ts](../apps/backend/src/lib/attachment-gc.ts) exports `runAttachmentGc(db)` (two-pass sweep) and `startAttachmentGc(app)` (15-min `setInterval`, runs on boot, `onClose` clears the handle, `running` flag short-circuits overlapping ticks). Orphan pass deletes rows + files where `messageId IS NULL AND createdAt < now() - INTERVAL '1 hour'` with belt-and-suspenders `AND messageId IS NULL` on the DELETE. ENOENT on unlink is tolerated as "already gone". Wired into `buildApp()` at [apps/backend/src/app.ts](../apps/backend/src/app.ts) after route registration and before `createSocketIO` so hook teardown order is correct. Covered by [apps/backend/tests/attachment-gc.test.ts](../apps/backend/tests/attachment-gc.test.ts) + [apps/backend/tests/attachment-gc-lifecycle.test.ts](../apps/backend/tests/attachment-gc-lifecycle.test.ts).

## S3 — hardening / pre-ship

- **SMTP for password-reset email (REQ-019 completion).** Today the reset token is logged (redacted in prod per task #7); no mail is sent. Wire nodemailer/SES. `docs/specs/s1-auth.md` §7 tracks it.
- ~~**Distributed rate-limit storage.**~~ Already shipped — pulled forward in S1 per [auth.ts:81-86](../apps/backend/src/auth.ts#L81-L86) comment ("using Redis here pulls the S3 'distributed rate-limit storage' forward"). [secondary-storage.ts](../apps/backend/src/secondary-storage.ts) wires a Redis-backed adapter; `rateLimit.storage = "secondary-storage"` at [auth.ts:113](../apps/backend/src/auth.ts#L113) routes better-auth's counters through it. Survives restarts, shareable across replicas. Verified 2026-04-20.
- ~~**CSRF double-submit token (REQ-146).**~~ Shipped 2026-04-19 on `feat/s3-hardening`: `csrf_token` cookie stamped on session creation by `proxyToBetterAuth` in `apps/backend/src/app.ts`, required as `X-CSRF-Token` header on every mutating `/api/v1/*` request via `csrfPreHandler` in `apps/backend/src/lib/csrf.ts`. Browser pass-through keyed on Origin/Referer so non-browser callers (testcontainers harness, curl probes) are exempt — this is the OWASP-endorsed shape. Web layer plumbs the header through `backend.ts#csrfHeaders()` across all five fetch wrappers in `chat-api.ts` + account/sessions/dms/friendship client modules.
- **S3 per-route rate-limit sweep — deferred less-critical routes.** `feat/s3-hardening` landed `config.rateLimit` on account/attachments/dms/invitations/sessions-delete-one. Skipped routes that already have hand-rolled Redis buckets (friendship, rooms create/join/mgmt/moderation, messages edit/delete, mutes, read-receipts) — the global 1000/min plugin cap still clamps them coarsely. A future sweep should unify on the plugin's per-route config (`apps/backend/src/routes/*.ts`): retire the custom `lib/*-rate-limit.ts` helpers so there's ONE rate-limit surface to reason about. Auth routes (sign-in/sign-up/forget-password) still ride better-auth's built-in limiter; brief §1c called for a manual tune-up (20/5min sign-in, 5/hr password-reset) that was deferred to avoid coupling with better-auth internals mid-event.
- ~~**Password change UI + "revoke all other sessions on password change".**~~ Shipped 2026-04-19 on `feat/s1-residual` as REQ-016: `/settings/password` page with current/new/confirm-new + "Sign out other sessions" checkbox (default on); posts to `/api/auth/change-password`; linked from Header. Covered by `apps/backend/tests/password-change.test.ts`.

- **CSRF Origin/Referer pass-through — document the trust boundary.** [apps/backend/src/lib/csrf.ts:141-153](../apps/backend/src/lib/csrf.ts#L141-L153) skips the double-submit check when neither `Origin` nor `Referer` is present. Justified as OWASP-endorsed (a request with no browser-sourced origin hint is not a CSRF vector — CSRF requires a browser the attacker tricked into riding victim cookies), and it lets the testcontainers harness + curl probes through without injecting the header into ~67 pre-existing tests. The residual risk: if a future browser bug ever lets a malicious page suppress both headers on a mutating request, the check degrades to "cookie-alone auth," which is exactly what CSRF defends against. Not a bug today; worth a line in the threat-model doc when one is written. No code change proposed — the pass-through is correct shape.
  - **Location**: [apps/backend/src/lib/csrf.ts](../apps/backend/src/lib/csrf.ts) `csrfPreHandler`.
  - **Raised**: 2026-04-19 at end of `feat/s3-hardening`.

- ~~**`BuildAppOptions.rateLimitGlobalMax` is test-only cruft in a production signature.**~~ Shipped 2026-04-20 on `chore/cleanup-build-options`: dropped the `BuildAppOptions` interface and the `options` parameter on `buildApp()`; the test override now lives behind a module-level `__setTestRateLimitGlobalMax(max)` setter in [apps/backend/src/app.ts](../apps/backend/src/app.ts) (double-underscore prefix flags it as test-only). [apps/backend/tests/rate-limit-global.test.ts](../apps/backend/tests/rate-limit-global.test.ts) flips it via `beforeAll`/`afterAll`. Production callers no longer see a knob they shouldn't touch.

- ~~**Hard-coded `better-auth.session_token=` regex in CSRF cookie stamping.**~~ Shipped 2026-04-20 on `chore/post-event-cleanup`: extracted as `SESSION_COOKIE_PREFIX` in [apps/backend/src/auth.ts](../apps/backend/src/auth.ts) and referenced by `proxyToBetterAuth` in [apps/backend/src/app.ts](../apps/backend/src/app.ts) via `c.startsWith(SESSION_COOKIE_PREFIX)`. A future better-auth dep bump that renames the cookie now fails typecheck/grep instead of silently dropping CSRF issuance.

## S2 → S3 (account deletion + GDPR export)

- ~~**Re-register with same email after account delete.**~~ Shipped 2026-04-20 on `feat/auth-correctness-debt`: the `DELETE /api/v1/users/me` transaction in [apps/backend/src/routes/account.ts](../apps/backend/src/routes/account.ts) now rewrites `email = 'deleted-<id>@tombstone.invalid'` and `username = 'deleted-<id8>'` alongside the `deletedAt` stamp. The new LOWER() unique indexes (migration 0010) then see the free originals and a fresh sign-up with the same credentials succeeds. `message.authorUsername` is a send-time snapshot (schema.ts:256) so historical messages keep rendering the real pre-delete author. Covered by [apps/backend/tests/account-delete-reregister.test.ts](../apps/backend/tests/account-delete-reregister.test.ts) + [account-delete-messages.test.ts](../apps/backend/tests/account-delete-messages.test.ts).

- ~~**Attachment cleanup on account delete.**~~ Shipped 2026-04-20 on `feat/s3-gc-and-moderation-rl` as the tombstoned-user pass in [apps/backend/src/lib/attachment-gc.ts](../apps/backend/src/lib/attachment-gc.ts). Runs alongside the orphan pass every 15 min: `SELECT a.id, a.storagePath FROM attachment a JOIN "user" u ON u.id = a.uploader_id WHERE u.deleted_at IS NOT NULL AND u.deleted_at < now() - INTERVAL '1 hour'`; unlinks + deletes scoped by `uploaderId`, so co-attachments uploaded by OTHER users onto the same message survive. Retention window = 1h (aligned with orphan pass — product did not request a longer grace period, so keep it simple). Covered by [apps/backend/tests/attachment-gc.test.ts](../apps/backend/tests/attachment-gc.test.ts) "tombstoned-user pass (R4.2)".

- ~~**Export download uses client-generated filename.**~~ Shipped 2026-04-20 on `feat/auth-correctness-debt`: `@fastify/cors` in [apps/backend/src/app.ts](../apps/backend/src/app.ts) now sets `exposedHeaders: ["content-disposition"]`, so the web layer reads the backend-proposed `user-data-export-<username>-<ts>.json` filename instead of falling back to the shorter client-generated form. Covered by [apps/backend/tests/export-cors-headers.test.ts](../apps/backend/tests/export-cors-headers.test.ts).

## Out of hackathon scope (referenced so reviewers don't flag as missing)

- **Username change (REQ-127)** — explicitly deferred in `docs/BRIEF.md`.
- **Email verification at signup** — `requireEmailVerification: false`; `user.emailVerified` column stays for better-auth compatibility but is always `false`. `docs/specs/s1-auth.md` §5.
- **Admin "force logout all users" tooling** — not in the REQ range.
- **XMPP federation (v3.docx §6 / REQ-180 range).** Deliberately deferred per [ADR-0002](adr/0002-no-xmpp.md). The MVP ships a façade at `/admin/federation` + [docs/FEDERATION.md](./FEDERATION.md) documenting the full enable path (Prosody + `mod_s2s` + custom Postgres storage module). Reason for deferral: TLS/DNS prerequisites exceed the 52h window and a half-shipped s2s daemon risks the `docker compose up` submission gate. Architecture is bridge-ready; the deferral is a scope call, not tech debt.

## ADR-0006 deviations (2026-04-18 retrofit)

Tracked deferrals from the v4 REQ-catalog retrofit. Each item has a pointer to where it currently lives and what shipping it would cost.

1. ~~**REQ-003 — case-insensitive email uniqueness.**~~ Shipped 2026-04-20 on `feat/auth-correctness-debt`: migration 0010 drops the original case-sensitive UNIQUE and adds a `user_email_ci_uq` expression index on `LOWER(email)`; better-auth 1.6.5 already lowercases at sign-up/sign-in (defense-in-depth against direct INSERT). Covered by [apps/backend/tests/user-email-case.test.ts](../apps/backend/tests/user-email-case.test.ts).
2. ~~**REQ-005 — case-insensitive username uniqueness.**~~ Shipped 2026-04-20 on `feat/auth-correctness-debt`: migration 0010 adds `user_username_ci_uq` on `LOWER(username)`; `databaseHooks.user.create.before` in [apps/backend/src/auth.ts](../apps/backend/src/auth.ts) normalizes the additionalField to lowercase at write time (better-auth does not normalize additionalFields). Covered by [apps/backend/tests/user-username-case.test.ts](../apps/backend/tests/user-username-case.test.ts).
3. **REQ-006 — password policy (12-char + top-10k blocklist).** Note: all test fixtures + seed use `"password1234"` which is on the blocklist; any implementation MUST rewrite fixtures in lockstep.
4. ~~**REQ-007 — passwordConfirm field.**~~ Shipped 2026-04-19 on `feat/s1-residual`: optional `passwordConfirm` + `.superRefine` in `packages/shared/src/dto.ts`; second password input on `apps/web/src/app/register/page.tsx`. Optional (not required) so the ~88 existing backend-test sign-up call sites keep working without a mechanical sweep; mismatch rejection is the contractual behaviour and is covered by `apps/backend/tests/register-validation.test.ts` REQ-007 describe block.
5. **REQ-008 — argon2id.** Written justification lives in [docs/adr/0009-no-argon2id.md](adr/0009-no-argon2id.md): better-auth 1.6.x hashes with scrypt via a non-pluggable `node:crypto` path; swapping to argon2id means unwinding ADR-0001 (stack pivot). Accepted deviation at the v3.docx §8 threat-model level. No test covers REQ-008; the ADR IS the artifact.
6. ~~**REQ-009 — /24 subnet rate limit.**~~ Shipped 2026-04-20: implemented as a Fastify preHandler on `POST /api/auth/sign-up/email` ([apps/backend/src/lib/register-rate-limit.ts](../apps/backend/src/lib/register-rate-limit.ts)) with IPv4 `/24` + IPv6 `/64` CIDR bucketing via Redis. better-auth 1.6.5 has no per-route `keyGenerator` hook (verified against `node_modules/better-auth/.../rate-limiter/index.mjs`), so the /24 rule runs one layer above `proxyToBetterAuth`; better-auth's per-/32 rule in `auth.ts customRules` stays as a second layer. Unit test: [apps/backend/src/lib/register-rate-limit.test.ts](../apps/backend/src/lib/register-rate-limit.test.ts).
7. **REQ-012 — per-email lockout.** Current `rate-limit.test.ts` exercises IP-scoped 429 only; per-email counter + `auth_locked` error code are S3 work (see ADR-0006 row).
8. **Auto-enroll is permanent, not a hotfix.** `auth.ts:117-143` + `register-auto-enroll.test.ts` stay indefinitely per s2-rooms.md R1 `[x]` (commit `ee0b136`). No deletion when S2 rooms (v4 REQ-025/REQ-026) land.
9. **s1-chat REQ-049 seed.** Seed script is demo infra, not a v4 REQ. Trace doesn't need to claim it.
10. ~~**v4 REQ-022 "Room description".**~~ Shipped 2026-04-20 on `feat/room-description-visibility` (commits `0e68e43`, `6ff0b87`, `5c850cd`, `d2c16ac`): backend PATCH widened to accept `description` + `visibility` with REQ-028 1000-char cap and `room.updated` fanout; UI surfaces description in `CreateRoomDialog` + `SettingsTab` with cap-toast handling.
11. ~~**REQ-037 — offline-to-online message backfill.**~~ Closed 2026-04-19 on `feat/s1-residual` by the integration test `apps/backend/tests/req-037-backfill.test.ts`. The existing watermark protocol (ADR-0003) + history API satisfy the REQ without new production code: on reconnect a client compares `subscribe.ack.roomHeadSeq` against its last-seen seq and backfills via `GET /api/v1/rooms/:id/messages?fromSeq=<lastSeen+1>&toSeq=<head>`.

## Wave A smoke residuals (2026-04-19)

Surfaced during manual verification of the Wave A merge set (s4-facade + s2-room-mgmt-ui + s2-presence + s2-account-gdpr) on `verify/wave-a-smoke`. All five submission-gate checks completed; four pass outright, one (presence UI) is green at the socket-protocol layer but broken at the member-list render layer. These items are intentionally deferred per the smoke brief's "no protocol changes" fence.

- ~~**Room-member roster endpoint missing — presence pills for non-self members never hydrate.**~~ Resolved 2026-04-19 on `feat/s2-presence` (commit `4b4168e`, merged as `5d43427`). New `GET /api/v1/rooms/:id/members` returns `{ id, username, displayName }[]` from `room_member` join `user`; `RoomClient.tsx` fetches it alongside history and replaced the `SEEDED_OTHER_MEMBERS` placeholder array, so non-self `PresencePill`s now bind to real UUIDs and react to `presence.changed`.

- ~~**Account-delete UX: password-confirm vs type-to-confirm drift.**~~ Resolved 2026-04-19 in `docs/specs/s2-account-gdpr.md` §5 — password-confirm is canonical. Implementation was already password-confirm (`apps/web/src/app/settings/account/page.tsx` dialog body); the drift was the early-agent brief's "Type DELETE" proposal, which the spec now explicitly rejects. Password re-auth proves identity (not just intent) and reuses better-auth's existing password check, so it's strictly stronger than a string-match gate.

- ~~**Export endpoint verb/path docs drift.**~~ Resolved 2026-04-19. `POST /api/v1/users/me/export` is the only form present across `docs/specs/s2-account-gdpr.md`, the protocol DTO comment, and the backend route. A grep across `docs/` for the stale `GET /api/v1/account/export` / `GET /users/me/export` shapes came up empty outside the self-describing FOLLOWUPS entry, which is now removed.

- ~~**Export filename pattern drift.**~~ Resolved 2026-04-19. Client fallback in `apps/web/src/lib/account-api.ts` now generates `user-data-export-<timestamp>.json`, matching the backend prefix (the backend still adds `<username>` when the header is exposed). Spec §5 documents the observed shape. Remaining work (CORS `exposedHeaders`) stays queued under "Export download uses client-generated filename" above.

- **Durable Playwright specs for Wave A gates (deferred).** Wave A ran as manual Playwright MCP passes. Gates 1/2/4/5 are stable surfaces with high regression risk and are good candidates for `tests/e2e/` specs that rerun at each submission-gate checkpoint. Wave B/C should inherit the test, not the MCP transcript.
  - **Fix shape**: four specs under `tests/e2e/` — (a) auth + room create/invite/rename/delete (Gate 2), (b) account export + delete + `[deleted user]` substitution (Gate 4), (c) federation façade admin page + 403 for non-admin (Gate 5), (d) docker-compose health smoke as a `pnpm test:e2e:smoke` runner that only hits `/health` and `/login`. Skip a UI presence-pill spec until the roster endpoint above lands — it would just encode the current bug. The existing seeded users (alice/bob/carol @herders.local / `hunter2hunter2`) cover auth setup; use separate `BrowserContext`s per role rather than Chrome+Firefox since the concurrency constraint was specific to Playwright MCP, not headless Playwright.
  - **Location**: new files in `tests/e2e/`; Playwright is already wired (`pnpm test:e2e` command from CLAUDE.md).
  - **Raised**: 2026-04-19 at end of Wave A smoke; explicitly accepted as scope creep by the human during the smoke pass.

## Wave B — S2 invitations (2026-04-19, `feat/invitations`)

- ~~**Q1 divergence: public-room members CAN invite.**~~ Verified 2026-04-20 — entry was self-contradictory (title said "CAN invite", body claimed "404s with room_not_found"). Current handler at [invitations.ts:93-97](../apps/backend/src/routes/invitations.ts#L93-L97) only role-gates when `visibility === 'private'`, and [invitations-create.test.ts:148-160](../apps/backend/tests/invitations-create.test.ts#L148-L160) (`REQ-089 R3 (Q1 divergence) public-room plain member MAY invite`) locks 201 for that path. No misleading-label bug exists; entry was stale.

- **Q4/Q5 deferrals: rate-limit (50 sent / 24h per inviter) + GC sweep (expired → pending).** Both deliberately out of S2 scope per pre-approved Q&A. Today expiry is enforced lazily at read-time (the R8 lazy-GC: `WHERE status='pending' AND expiresAt > now()` in every list + 409-duplicate check); no background job flips `pending → expired`, and there is no cap on invitations per-inviter-per-24h. Lazy-GC is correct for the 24h window; the sweep just keeps the DB tidy over weeks. Rate-limit is REQ-157-adjacent moderation work.
  - **Location**: [apps/backend/src/routes/invitations.ts](../apps/backend/src/routes/invitations.ts) (handler is where a Fastify `rateLimit` preHandler would slot in), [docs/specs/s2-invitations.md §4 R8 + §7 Out-of-scope](specs/s2-invitations.md).
  - **Fix shape**: (a) `fastify-rate-limit` per-inviter bucket keyed on `inviterId`, window=86400, max=50, 429 with `invite_rate_limited`; (b) cron job (Redis-scheduled or Fastify task) `UPDATE room_invite SET status='expired' WHERE status='pending' AND expiresAt < now()`. Neither changes contracts — both are server-side only.
  - **Raised**: 2026-04-19.

- ~~**`INVITATIONS_ENFORCE_BAN` feature flag — remove once agent A's ban migration lands.**~~ Shipped 2026-04-20 on `chore/post-event-cleanup`: env entry, `probeRoomBan` startup helper, and runtime branching all removed from [apps/backend/src/env.ts](../apps/backend/src/env.ts) and [apps/backend/src/routes/invitations.ts](../apps/backend/src/routes/invitations.ts); the ban-check is now unconditional (agent A's `0007_room_roles.sql` shipped `room_ban`). Spec [s2-invitations.md §5/§7](./specs/s2-invitations.md) updated in lockstep; `invitations-create.test.ts` REQ-089 ban test name shortened.

- ~~**Better-auth `/sign-up/email` rate-limit bleed across invitation test files (re-observed).**~~ Shipped 2026-04-20 on `chore/test-infra` alongside the root-cause fix at the batched-verification entry above. The invitation-file re-observation is the same bucket-exhaustion symptom; the test-mode `customRules.max = 10000` bump (verified against better-auth 1.6.5's function-form rule in `node_modules/better-auth/dist/api/rate-limiter/index.mjs:136`) covers all chained suites equally, with `__setTestSignUpMaxOverride` as the escape-hatch for the one test that has to prove the real 5-per-hour 429 path.
  - **Location**: [apps/backend/src/auth.ts](../apps/backend/src/auth.ts) `rateLimit.customRules`; [apps/backend/tests/db-helpers.ts](../apps/backend/tests/db-helpers.ts) `flushRedis()`.
  - **Raised**: 2026-04-19 during feat/invitations gate run (pre-existing, not a regression).

- **e2e spec `tests/e2e/s2-invitations.spec.ts` currently skips when backend is down.** Added a `beforeAll` health probe at `http://localhost:4000/health` so the spec is safe to run without the dev stack up (the test `test.skip`s with an actionable message). This is the documented "no protocol changes, no silent CI breakage" pattern from the smoke brief, not a gap — but the Chrome+Firefox dual-browser run (per `feedback-playwright-multi-user` memory) is not wired yet; today both contexts come from the same browser. If the final gate checkpoint needs true cross-browser coverage, either swap the second `browser.newContext()` for a `firefox.launch()` call or add a second `projects` entry in [playwright.config.ts](../playwright.config.ts).
  - **Location**: [tests/e2e/s2-invitations.spec.ts](../tests/e2e/s2-invitations.spec.ts); [playwright.config.ts](../playwright.config.ts).
  - **Raised**: 2026-04-19 at the end of feat/invitations.

## Wave 2 — catalog + emoji + DM unread (2026-04-19, `feat/catalog-emoji-dm-unread`)

- ~~**§2.7 DM unread parity — audit-only.**~~ Resolved 2026-04-19. Verified in [apps/backend/tests/dm-unread-parity.test.ts](../apps/backend/tests/dm-unread-parity.test.ts): `GET /api/v1/rooms/me` exposes `lastReadSeq` + `roomHeadSeq` (bigint-on-the-wire strings, ADR-0003) for `kind='dm'` rooms identically to `kind='group'`, and `POST /api/v1/rooms/:id/read` zeroes unread for DMs without a special-case branch. No production code needed patching; the R4 handler was already DM-parity-safe by construction (no `kind='group'` filter in the SELECT). §2.7 is entirely a coverage gap closure.

- **Dual-browser smoke for §2.4.3 catalog search + §2.5.2 emoji picker.** Deferred per batched-smoke SOP to the next docker-compose checkpoint. Steps: (a) Alice in Chrome loads `/rooms/browse`, types "gen" in the search input → filtered list arrives within ~500 ms and excludes any private room; (b) Alice in a room composer clicks the emoji trigger → picker popover renders (Suspense fallback visible on cold load), picks 🎉, emoji splices at caret, Send submits body with emoji intact; (c) verify the private-room leak-proof assertion holds in the browser (admin-search for a private-room name Alice doesn't belong to returns no result). Dual-browser (Chrome+Firefox per `feedback-playwright-multi-user` memory) not required for these flows since they're single-user — single-browser multi-context is fine. Raised 2026-04-19 at end of wave 2.

## S3 — chat-surface polish (2026-04-20, `feat/chat-surface-polish`)

- **Parallel-fork backend test flake: `rooms-create` + `attachments-upload` + `rooms-join-rate-limit`.** During final verification of `feat/chat-surface-polish`, a full `pnpm --filter backend test:run` reported 4 failures in these three files. Every failing file passes green in isolation; re-running the full suite returns 606/606 green. Signature matches memory `feedback-vitest-one-buildapp.md` (multiple `buildApp()` calls race better-auth init; sign-ups return 200 but write no user row) *and* the existing "Backend parallel-DB FK race across test files" entry above. None of the failing files are touched by this branch and they sit on the DO-NOT-EDIT boundary list (`routes/rooms.ts`, `routes/attachments.ts`). Log-and-park: fix shape lives in the existing entries (tests/setup.ts truncation ordering + vitest pool config). Raised 2026-04-20 at end of chat-surface-polish final verification.
- **Dual-browser smoke for REQ-212 admin delete + REQ-213 paperclip attach + REQ-215 AFK/offline suffix.** Deferred per batched-smoke SOP to the next docker-compose checkpoint. Steps: (a) Alice (owner, Chrome) creates public room, Bob (member, Firefox) joins and posts a message; Alice hovers Bob's row → "Delete" action visible, "Edit" absent; Alice clicks Delete → Confirm → both tabs show the tombstone with `deletedByRole='admin'` semantics. (b) Alice clicks the composer paperclip → file picker opens → selecting a txt file lands it in the preview row → Send uploads and the chip renders in both tabs. (c) Bob goes AFK (timer passes presence threshold) → Alice's MemberList row shows "Bob (AFK)" within 2s; Bob closes the tab → row shows "Bob (offline)". Playwright scaffolding lives at [tests/e2e/s2-admin-delete.spec.ts](../../tests/e2e/s2-admin-delete.spec.ts) and [tests/e2e/s2-attach-button.spec.ts](../../tests/e2e/s2-attach-button.spec.ts); no e2e scaffolding for REQ-215 yet (component test covers it). Raised 2026-04-20 at end of chat-surface-polish.
